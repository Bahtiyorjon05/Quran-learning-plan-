import { Opening } from "@/components/brand/opening";

/**
 * The dashboard is what an installed icon opens, and it is server-rendered.
 * This is what stands in its place while it is being built.
 */
export default function Loading() {
  return <Opening />;
}
